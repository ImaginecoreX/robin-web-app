import '../globals.css'

export const metadata = {
  title: 'Register',
  description: 'Register your details in system to access features',
}

export default function RootLayout({ children }) {
  return (
    
        <main>
        {children}
        </main>

  )
}